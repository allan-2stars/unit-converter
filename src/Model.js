const initModel = {
  // The Data model initially has 2 parts
  // left, and right.
  valueLeft: 0,
  valueRight: 0,
  unitLeft: 'Celsius',
  unitRight: 'Fahrenheit',
  // if the input is initialized from Left
  // then set sourceLeft is true otherwise from Right,
  // set sourceLeft is false
  sourceLeft: true
};

export default initModel;
